import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    btn: {
        backgroundColor: '#ec910a',
        height: 60,
        width: 300,
        borderWidth: 1.5,
        alignItems: 'center',
        justifyContent: 'center',
        borderColor: '#ec910a',
        marginTop: 30
    }
});